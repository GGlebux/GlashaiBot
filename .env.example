# ============================================================
#  Глашатай 📯 — пример конфигурации.
#  Скопируй в .env и заполни своими значениями:  cp .env.example .env
# ============================================================

# --- Telegram ---
# Токен бота от @BotFather
BOT_TOKEN=123456:ABC-DEF...
# Telegram user-id админов через запятую (узнать у @userinfobot)
ADMIN_IDS=11111111,22222222

# --- Лимиты использования ---
# Сколько голосовых/кружков может обработать один юзер в сутки
DAILY_LIMIT=20
# Максимальная длительность аудио в секундах (что дольше — отклоняем)
MAX_AUDIO_SECONDS=600

# --- Движок распознавания речи (STT) ---
# whisper      — локальный faster-whisper (по умолчанию, бесплатно, без квот)
# salutespeech — облако Сбера (нужен подключённый пакет, иначе 402)
STT_BACKEND=whisper
# Модель Whisper: tiny | base | small | medium | large-v3
#   1 ГБ RAM → tiny/base; 2 ГБ → small (рекомендуется, качество заметно выше)
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8
WHISPER_LANGUAGE=ru

# --- SaluteSpeech (STT, распознавание речи) — нужен только при STT_BACKEND=salutespeech ---
# ВАРИАНТ 1: «Ключ авторизации» (Authorization key) — длинная Base64-строка
#            из кабинета developers.sber.ru -> SaluteSpeech. НЕ Client ID (UUID)!
SALUTE_SPEECH_AUTH_KEY=
# ВАРИАНТ 2 (если нет готового ключа): задай Client ID и Client Secret —
#            ключ соберётся автоматически. Заполняй ЛИБО вариант 1, ЛИБО вариант 2.
SALUTE_SPEECH_CLIENT_ID=
SALUTE_SPEECH_CLIENT_SECRET=
# Scope: для физлиц SALUTE_SPEECH_PERS, для юрлиц SALUTE_SPEECH_CORP
SALUTE_SPEECH_SCOPE=SALUTE_SPEECH_PERS

# --- GigaChat (LLM, краткое содержание) ---
# ВАРИАНТ 1: «Ключ авторизации» (Authorization key) — длинная Base64-строка.
GIGACHAT_AUTH_KEY=
# ВАРИАНТ 2: Client ID + Client Secret (соберём ключ сами).
GIGACHAT_CLIENT_ID=
GIGACHAT_CLIENT_SECRET=
# Scope: для физлиц GIGACHAT_API_PERS
GIGACHAT_SCOPE=GIGACHAT_API_PERS
# Модель по качеству: GigaChat (Lite) < GigaChat-Pro < GigaChat-Max (лучшая).
# У Freemium: Lite ~1 млн токенов/год, Pro и Max — по 50 тыс. (хватает на десятки
# саммари). Для лучшего качества ставь GigaChat-Max.
GIGACHAT_MODEL=GigaChat-Max
# Запасная модель, если у основной кончился лимит (чтобы бот не падал).
# Пусто = без подмены. По умолчанию — бюджетная GigaChat (Lite).
GIGACHAT_FALLBACK_MODEL=GigaChat

# --- База данных (PostgreSQL) ---
POSTGRES_DB=glashatai
POSTGRES_USER=glashatai
POSTGRES_PASSWORD=change-me-please
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# --- Redis (очередь arq + сессии /begin) ---
REDIS_URL=redis://redis:6379/0

# --- Админ-панель ---
# Домен для HTTPS (Caddy выпустит сертификат) и входа через Telegram.
# IP не подходит — нужен домен (можно бесплатный, например DuckDNS).
DOMAIN=your-name.duckdns.org
# Публичный адрес админки = https://<DOMAIN>. Telegram Login требует именно https.
ADMIN_BASE_URL=https://your-name.duckdns.org
# Секрет для подписи сессионных cookie (любая длинная случайная строка)
ADMIN_SECRET_KEY=change-this-to-a-long-random-string
# Внутренний порт админки (наружу торчит Caddy на 443; обычно менять не нужно)
ADMIN_PORT=8080

# --- Деплой (прод, docker-compose.yml) ---
# Образ из Docker Hub, который тянут сервисы и обновляет Watchtower
DOCKER_IMAGE=yourdockerhubuser/glashatai:latest

# --- Прочее ---
LOG_LEVEL=INFO
TZ=Europe/Moscow
